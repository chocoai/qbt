package com.qbt.persistent.mapper;

import java.util.List;

import com.qbt.common.entity.PageEntity;
import com.qbt.persistent.entity.TicketPackageOrder;

public interface TicketPackageOrderMapper {
	
    int insert(TicketPackageOrder record);

    TicketPackageOrder selectById(Integer id);

    int updateById(TicketPackageOrder record);

    /**
     * 分页查询
     * @param pageEntity
     * @return
     */
    List<TicketPackageOrder> findByPage(PageEntity<TicketPackageOrder> pageEntity);
}